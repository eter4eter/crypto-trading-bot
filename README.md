# Crypto Trading Bot

Автоматический торговый бот для биржи Bybit с поддержкой корреляционных стратегий.

## Особенности

- 📈 **Корреляционные стратегии**: Анализ движений доминирующей и целевой валюты
- ⚡ **Real-time данные**: WebSocket соединения для мгновенного отклика
- 🛡 **Управление рисками**: Контроль stop-loss, take-profit, проскальзывания
- 📊 **Мониторинг**: Полная статистика торговли и логирование
- 📨 **Уведомления**: Telegram бот для оповещений (опционально)
- 🐳 **Docker поддержка**: Легкое развертывание в контейнере

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/eter4eter/crypto-trading-bot.git
cd crypto-trading-bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Скопируйте пример конфигурации:

```bash
cp .env.example .env
```

Отредактируйте файл `.env` и укажите:

```env
BYBIT_API_KEY=ваш_api_key
BYBIT_API_SECRET=ваш_api_secret
BYBIT_TESTNET=true  # для тестирования
BYBIT_DEMO_MODE=true
```

### 4. Настройка стратегий

Отредактируйте файл `config/config.json` согласно вашим торговым стратегиям.

## Запуск

### Локальный запуск

```bash
python main.py
```

### Запуск через Docker

```bash
# Сборка образа
docker-compose build

# Запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f crypto-bot
```

### Запуск через Makefile

```bash
# Посмотреть доступные команды
make help

# Установка зависимостей
make install

# Запуск тестов
make test

# Запуск бота
make run
```

## Конфигурация

### Основные параметры

- `api_key` / `api_secret`: API ключи Bybit
- `testnet`: Режим тестовой сети
- `demo_mode`: Демо режим (без реальных сделок)
- `max_stop_loss_streak`: Максимальное количество последовательных stop-loss

### Настройка стратегий

Каждая стратегия конфигурируется в файле `config/config.json`:

```json
{
  "pairs": [
    {
      "name": "BTC-ETH-Correlation",
      "dominant_pair": "BTCUSDT",
      "target_pair": "ETHUSDT",
      "tick_window": 30,
      "timeframe": "1",
      "dominant_threshold": 0.5,
      "target_max_threshold": 0.3,
      "leverage": 5,
      "direction": 0,
      "reverse": 0
    }
  ]
}
```

### Параметры стратегии

- `dominant_pair`: Доминирующая пара (обычно BTC)
- `target_pair`: Целевая пара для торговли
- `tick_window`: Количество тиков для анализа (0 = последняя свеча)
- `timeframe`: Таймфрейм ("1s", "5s", "1", "5", "15", "60", "D")
- `leverage`: Плечо (1 = spot, >1 = futures)
- `direction`: Направление (-1=short, 0=both, 1=long)
- `reverse`: Логика реверса (0=прямая, 1=обратная)

## Архитектура

```
src/
├── api/              # Bybit API интеграция
├── strategy/        # Торговые стратегии
├── trading/         # Управление позициями
├── storage/         # База данных
├── monitoring/      # Мониторинг и статистика
└── notifications/   # Уведомления (Telegram)
```

## Мониторинг

Бот предоставляет подробную статистику:

- Общие показатели торговли
- Статистика по стратегиям
- API статистика (запросы, ошибки)
- WebSocket состояние

## Безопасность

- API ключи хранятся в переменных окружения
- Поддержка testnet для тестирования
- Demo режим для безопасного тестирования

## Отладка

Для отладки установите уровень логирования на `DEBUG` в `config/config.json`:

```json
{
  "api": {
    "logging_level": "DEBUG"
  }
}
```

## Лицензия

MIT License

## Вклад

Для вклада в развитие проекта:

1. Сделайте fork репозитория
2. Создайте ветку для новой функции
3. Напишите тесты
4. Отправьте Pull Request

## Поддержка

При вопросах или проблемах создайте issue в GitHub.

---

⚠️ **Предупреждение**: Торговля криптовалютами связана с высокими рисками. Тестируйте стратегии на testnet перед реальной торговлей.
