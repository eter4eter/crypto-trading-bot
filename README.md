# Crypto Trading Bot

Автоматический торговый бот для биржи Bybit с поддержкой мультисигнальных стратегий и разных временных интервалов.

## Особенности

- 🎯 **Мультисигнальные стратегии**: Несколько сигналов с разными временными интервалами в одной стратегии
- 📈 **Корреляционная торговля**: Анализ движений индексных и целевых пар
- ⚡ **Real-time данные**: WebSocket + polling для разных временных интервалов
- 🛡 **Управление рисками**: Автоматическая нормализация qty, контроль проскальзывания
- 📊 **Детальная статистика**: Мониторинг торговли, API статистика, производительность
- 📨 **Telegram уведомления**: Оповещения о сигналах и результатах сделок
- 🐳 **Docker поддержка**: Легкое развертывание в контейнере
- ⚙️ **Гибкая конфигурация**: Per-symbol категории (spot/linear), адаптивные параметры

## Архитектура системы

### Поток данных

```
Bybit API → GlobalMarketDataManager → MultiSignalStrategy → PositionManager → Orders
     ↓              ↓                      ↓                    ↓
WebSocket      Polling (1s,5s,...)    Signal Analysis      Risk Management
     ↓              ↓                      ↓                    ↓
Real-time      Sub-minute            Correlation Logic   Order Normalization
```

## Конфигурация

### Структура config.json

```json
{
  "api": {
    "api_key": "your_api_key",
    "api_secret": "your_secret",
    "testnet": true,
    "demo_mode": true
  },
  "global": {
    "max_stop_loss_trades": 3,
    "logging_level": "INFO",
    "database_path": "data/trading.db"
  },
  "strategies": {
    "strategy_name": {
      "trade_pairs": ["ETHUSDT", "SOLUSDT"],
      "leverage": 2,
      "tick_window": 10,
      "price_change_threshold": 0.05,
      "stop_take_percent": 0.005,
      "position_size": 50,
      "direction": 0,
      "enabled": true,
      "signals": {
        "signal_name": {
          "index": "BTCUSDT",
          "frame": "1",
          "tick_window": 30,
          "index_change_threshold": 0.02,
          "target": 0.04,
          "direction": 0,
          "reverse": 0
        }
      }
    }
  },
  "telegram": {
    "enabled": false,
    "bot_token": "",
    "chat_id": ""
  }
}
```

### Параметры стратегии

**Основные параметры:**
- `trade_pairs`: Список торговых пар для исполнения сигналов
- `leverage`: Плечо (1 = spot, >1 = linear futures)
- `tick_window`: Окно анализа по умолчанию
- `price_change_threshold`: Максимальное проскальзывание (%)
- `stop_take_percent`: Размер TP/SL (%)
- `position_size`: Размер позиции в USDT
- `direction`: Направление торговли (-1=short, 0=both, 1=long)
- `enabled`: Включена ли стратегия

**Параметры сигнала:**
- `index`: Индексная пара (например, BTCUSDT)
- `frame`: Временной интервал ("1s", "5s", "1", "5", "15", "30", "60", "D", "W", "M")
- `tick_window`: Окно анализа для этого сигнала (0 = последние 2 свечи)
- `index_change_threshold`: Минимальное изменение индекса для срабатывания (%)
- `target`: Максимальное изменение цели (если больше — сигнал игнорируется) (%)
- `direction`: Направление сигнала (-1=short, 0=both, 1=long)
- `reverse`: Инвертировать логику (0=прямая, 1=обратная)

### Временные интервалы

**WebSocket (стандартные):**
- Минутные: "1", "3", "5", "15", "30", "60", "120", "240", "360", "720"
- Дневные: "D" (день), "W" (неделя), "M" (месяц)

**Polling (субминутные):**
- Секундные: "1s", "5s", "10s", "15s", "30s"

### Примеры стратегий

**1. Быстрая корреляционная стратегия:**
```json
"BTC_ETH_fast": {
  "trade_pairs": ["ETHUSDT"],
  "leverage": 3,
  "position_size": 25,
  "signals": {
    "btc_impulse": {
      "index": "BTCUSDT",
      "frame": "1s",
      "tick_window": 5,
      "index_change_threshold": 0.08,
      "target": 0.05,
      "direction": 0,
      "reverse": 0
    }
  }
}
```

**2. Мультисигнальная стратегия:**
```json
"multi_timeframe": {
  "trade_pairs": ["WIFUSDT"],
  "signals": {
    "short_term": {
      "index": "BTCUSDT",
      "frame": "1s",
      "index_change_threshold": 0.01
    },
    "medium_term": {
      "index": "ETHUSDT", 
      "frame": "5",
      "index_change_threshold": 0.02
    }
  }
}
```

**3. Обратная логика (контртренд):**
```json
"reversal_strategy": {
  "signals": {
    "btc_rejection": {
      "index": "BTCUSDT",
      "frame": "1",
      "reverse": 1,  // Обратная логика
      "index_change_threshold": 0.3,
      "target": 0.1
    }
  }
}
```

## Управление рисками

### Автоматическая нормализация ордеров

Бот автоматически:
- Получает спецификации инструментов (qtyStep, tickSize, minNotional)
- Нормализует количество по шагу инструмента
- Округляет TP/SL цены по минимальному тику
- Проверяет минимальный номинал сделки

### Контроль проскальзывания

- `price_change_threshold`: Максимальное отклонение цены от расчетной (%)
- Сигналы с превышением порога игнорируются
- Текущая цена запрашивается перед каждым ордером

### Stop-loss защита

- `max_stop_loss_trades`: Максимальное количество подряд SL
- Автоматическая приостановка торговли при превышении
- Сброс счетчика через 24 часа или при прибыльной сделке

## API лимиты и масштабирование

### Ограничения Bybit

- **HTTP API**: 120 запросов/сек на UID
- **WebSocket**: 1000 соединений на IP
- **Рекомендуемый лимит**: до 30 торговых пар

### Оптимизация производительности

1. **Кэширование**: Спецификации инструментов кэшируются на 5 минут
2. **WebSocket приоритет**: Используется для минутных интервалов
3. **Polling группировка**: Секундные интервалы группируются по частоте
4. **Мониторинг лимитов**: Отслеживание заголовков `X-Bapi-Limit-Status`

## Мониторинг и логирование

### Уровни логирования

- **DEBUG**: Детальная отладочная информация
- **INFO**: Обычная работа, сигналы, сделки
- **WARNING**: Предупреждения, проблемы с соединением
- **ERROR**: Критические ошибки

### Статистика торговли

```
📍 ═══ Cycle 120 Status ═══
  Open positions: 2
  Total trades: 15
  Win rate: 73.3%
  SL streak: 0

  Strategies:
    [BTC-ETH-cross] Signals: 45, Generated: 15
    
  API Stats:
    Requests: 1,234
    Errors: 3 (0.24%)
    
  WebSocket Stats:
    Connected: true
    Messages: 5,432
    Subscriptions: 8
```

### База данных

Вся информация сохраняется в SQLite:
- **orders**: История всех ордеров
- **signals**: Все сгенерированные сигналы  
- **daily_stats**: Ежедневная статистика
- **trade_performance**: Детальная аналитика

## Telegram уведомления

### Настройка

1. Создайте бота через @BotFather
2. Получите `bot_token`
3. Найдите свой `chat_id` (@userinfobot)
4. Обновите конфигурацию:

```json
"telegram": {
  "enabled": true,
  "bot_token": "1234567890:XXXXXXXXXXXXXXXXXXXXXXX",
  "chat_id": "123456789"
}
```

### Типы уведомлений

- **🎯 Новые сигналы**: Детали входа в позицию
- **✅ Закрытие позиций**: P&L, причина закрытия
- **❌ Ошибки**: Критические проблемы
- **📊 Ежедневные отчеты**: Статистика за день

### Demo режим

Для безопасного тестирования:

```json
{
  "api": {
    "testnet": false,
    "demo_mode": true
  },
  "strategies": {
    "test_strategy": {
      "position_size": 5,  // Минимальный размер
      "enabled": true
    }
  }
}
```

## Docker развертывание

### docker-compose.yml

```yaml
version: '3.8'
services:
  crypto-bot:
    build: .
    container_name: crypto-trading-bot
    environment:
      - BYBIT_API_KEY=${BYBIT_API_KEY}
      - BYBIT_API_SECRET=${BYBIT_API_SECRET}
      - BYBIT_DEMO_MODE=true
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
```

### Команды

```bash
# Запуск
docker-compose up -d

# Логи
docker-compose logs -f crypto-bot

# Остановка
docker-compose down
```
### Диагностика

```bash
# Включить debug логи
export LOG_LEVEL=DEBUG

# Проверить конфигурацию
python -c "from src.config import Config; print(Config.load('config/config.json'))"

# Тест API соединения
python -c "import asyncio; from src.api.bybit_client import BybitClient; asyncio.run(BybitClient('k','s',True,True).get_wallet_balance())"
```

## Производительность

### Системные требования

- **RAM**: 512MB минимум, 1GB рекомендуется
- **CPU**: 1 ядро (2+ для >10 пар)
- **Диск**: 100MB + логи
- **Сеть**: Стабильное соединение с низкой латентностью

## Безопасность
### Переменные окружения

```bash
# .env файл
BYBIT_API_KEY=your_api_key
BYBIT_API_SECRET=your_api_secret
BYBIT_TESTNET=false 
BYBIT_DEMO_MODE=true

# Опциональные
LOG_LEVEL=INFO
LOG_DIR=logs
DATABASE_PATH=data/trading.db

# Telegram
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_CHAT_ID=your_chat_id
```